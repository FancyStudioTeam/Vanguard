{
	"$schema": "https://turborepo.com/schema.json",
	"tasks": {
		"build": {
			"dependsOn": [
				"^build"
			],
			"env": [
				"POSTGRE_SQL_DATABASE_URL"
			],
			"inputs": [
				"$TURBO_DEFAULT$"
			],
			"outputs": [
				"build/**",
				"dist/**"
			]
		}
	}
}
