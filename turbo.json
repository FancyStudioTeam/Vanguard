{
	"$schema": "https://turborepo.com/schema.json",
	"tasks": {
		"build": {
			"env": [
				"DISCORD_TOKEN"
			],
			"inputs": [
				"$TURBO_DEFAULT$"
			],
			"outputs": [
				".next/**",
				"dist/**"
			]
		}
	}
}
