{
	"$schema": "https://turborepo.com/schema.json",
	"tasks": {
		"build": {
			"dependsOn": [
				"^build"
			],
			"env": [
				"DISCORD_TOKEN"
			],
			"inputs": [
				"$TURBO_DEFAULT$"
			],
			"outputs": [
				".next/**",
				"build/**",
				"dist/**"
			]
		}
	}
}
